# OpenClaw Minecraft Bot Configuration
# Copy this file to .env and customize for your deployment
#
# IMPORTANT: Never commit .env to git (already in .gitignore)
# IMPORTANT: Never hardcode these values in bot.js

# Bot Identity (REQUIRED)
# This sets the bot's Minecraft username AND command prefix
# Examples: Nova_AI → "nova help", Claude_Bot → "claude help"
BOT_USERNAME=Nova_AI

# Minecraft Server (REQUIRED)
MC_HOST=localhost
MC_PORT=25565

# Personality File (OPTIONAL)
# Path to SOUL.md file that defines bot's personality, values, and boundaries
SOUL_PATH=/data/.openclaw/workspace/SOUL.md

# Usage:
# 1. Copy this file: cp .env.example .env
# 2. Edit .env with your values
# 3. Load and run: export $(cat .env | xargs) && node bot.js
#
# Or use the launcher script: ./start-bot.sh
