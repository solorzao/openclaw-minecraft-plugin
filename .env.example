# OpenClaw Minecraft Bot Configuration
# Copy this file to .env and customize for your deployment
#
# IMPORTANT: Never commit .env to git (already in .gitignore)

# ============================================
# REQUIRED SETTINGS
# ============================================

# Bot Identity (Minecraft username)
BOT_USERNAME=My_Bot

# Minecraft Server
MC_HOST=localhost
MC_PORT=25565

# ============================================
# OPTIONAL SETTINGS
# ============================================

# Data Directory
# Where bot stores state.json, events.json, and reads commands.json
# Default: ./data/ (relative to repo root)
# BOT_DATA_DIR=./data

# Online Mode Server Authentication
# Only needed if server has online-mode=true (verifies Minecraft account)
# Leave commented for offline mode servers
# MC_USERNAME=your-email@example.com
# MC_PASSWORD=your-password

# ============================================
# SETUP INSTRUCTIONS
# ============================================
#
# 1. Copy this file:
#    cp .env.example .env
#
# 2. Edit .env with your server details
#
# 3. Run the bot:
#    export $(cat .env | xargs) && npm start
#
# Or use the launcher script:
#    ./start-bot.sh
